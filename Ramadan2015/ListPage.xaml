<Page
    x:Class="Ramadan2015.ListPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Ramadan2015"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
	DataContext="{Binding Main, Source={StaticResource Locator}}" RequestedTheme="Dark">
	<Page.Resources>
		<local:DateFormatConverter x:Key="ConverteDate"/>
		<local:TimeFormatConverter x:Key="ConverteTime"/>

		<DataTemplate x:Key="ItemListTemplate01">
			<Grid Background="#2C000000" Width="{StaticResource ContentDialogContentLandscapeWidth}">
				<Grid.ColumnDefinitions>
					<ColumnDefinition/>
					<ColumnDefinition Width="3*"/>
				</Grid.ColumnDefinitions>
				<Grid Background="#34000000">
					<StackPanel VerticalAlignment="Center">
						<TextBlock Text="DAY" FontSize="{StaticResource TextStyleLargeFontSize}" FontFamily="Segoe WP Light" FontWeight="Light" HorizontalAlignment="Center"/>
						<Rectangle Height="1" Margin="15,2" Stroke="#CA000000" StrokeThickness="0.5"/>
						<TextBlock Text="{Binding Serial}" FontSize="{StaticResource TextStyleLargeFontSize}" FontFamily="Segoe WP Light" FontWeight="Light" HorizontalAlignment="Center"/>
					</StackPanel>
				</Grid>
				<Grid Grid.Column="1">
					<Grid.RowDefinitions>
						<RowDefinition/>
						<RowDefinition/>
						<RowDefinition/>
					</Grid.RowDefinitions>
					<Grid Grid.Row="0" >
						<StackPanel Margin="15, 5, 0, 2.5" Orientation="Horizontal" VerticalAlignment="Center">
							<Image Source="Assets/Resources/date_icon.png" Stretch="Fill" VerticalAlignment="Center" Width="{StaticResource ListViewItemReorderHintThemeOffset}"/>
							<TextBlock Text="{Binding Path=Date, Mode=OneWay, Converter={StaticResource ConverteDate}, ConverterParameter=\{0:D\}}" FontSize="{StaticResource TextStyleLargeFontSize}" VerticalAlignment="Center" Margin="10,0,0,0" />
						</StackPanel>
					</Grid>
					<Grid Grid.Row="1" Background="#3FE6F0A8">
						<StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="15,2.5,0,2.5">
							<Image Source="Assets/Resources/Sehri.png" Stretch="Fill" VerticalAlignment="Center" Width="{StaticResource ListViewItemReorderHintThemeOffset}" />
							<TextBlock Text="Sahri " FontSize="{StaticResource TextStyleLargeFontSize}" VerticalAlignment="Center" Margin="10,0,0,0" />
							<TextBlock Text="{Binding Path=Sahri, Mode=OneWay, Converter={StaticResource ConverteTime}, ConverterParameter=\{0: h:mm tt\}}" FontSize="{StaticResource TextStyleLargeFontSize}" VerticalAlignment="Center" Margin="10,0,0,0" />

						</StackPanel>
					</Grid>
					<Grid Grid.Row="2">
						<StackPanel Margin="15,2.5,0,5" Orientation="Horizontal" VerticalAlignment="Center">
							<Image Source="Assets/Resources/Iftar.png" Stretch="Fill" VerticalAlignment="Center" Width="{StaticResource ListViewItemReorderHintThemeOffset}"/>
							<TextBlock Text="Ifter " FontSize="{StaticResource TextStyleLargeFontSize}" VerticalAlignment="Center" Margin="10,0,0,0" />
							<TextBlock Text="{Binding Path=Iftar, Mode=OneWay, Converter={StaticResource ConverteTime}, ConverterParameter=\{0: h:mm tt\}}" FontSize="{StaticResource TextStyleLargeFontSize}" VerticalAlignment="Center" Margin="10,0,0,0" />
						</StackPanel>
					</Grid>
				</Grid>
			</Grid>
		</DataTemplate>
	</Page.Resources>
	<Page.Background>
		<ImageBrush Stretch="Fill" ImageSource="Assets/Resources/BG.jpg"/>
	</Page.Background>


	<Grid>
		<Grid Margin="0, 10, 0, 10">
			<Grid.RowDefinitions>
				<RowDefinition Height="1*"/>
				<RowDefinition Height="10*"/>
			</Grid.RowDefinitions>
			<TextBlock Text="Sahri Iftar Time" Foreground="#FF531111" FontSize="{StaticResource TextStyleExtraExtraLargeFontSize}" FontFamily="Segoe UI Light" FontWeight="Light" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,0,0,15"/>
			<Grid  Grid.Row="1">
				<ListBox Background="{x:Null}" ItemTemplate="{StaticResource ItemListTemplate01}" ItemsSource="{Binding LoadData}"/>
			</Grid>

		</Grid>
	</Grid>
</Page>
																												   